{% extends 'base.html.twig' %}

{% block body %}

    <h1>{{ vm.pageTitle }}</h1>

    <div class="mt-4">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="mt-4">
            <button type="submit" class="btn btn-success">{{ vm.labelActionAdd }}</button>
        </div>
        {{ form_end(form) }}
    </div>

{% endblock %}
